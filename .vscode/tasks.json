{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run npm script",
            "command": ". ~/.nvm/nvm.sh; nvm use; npm start",
            "type": "shell",
            "runOptions": {
                "runOn": "folderOpen"
            }
        }
    ]
}